export enum SwaggerErrorDescriptions {
   BadRequest = 'Some information in the body of the request is incorrect or has a bad format.',
   NotFound = 'The item was not found.',
   Unauthorized = 'There is no enough information of the user that is trying to do the action.',
   Forbidden = 'The user trying to do the action has no enough permissions to perform it.',
}
